import { StyleSheet } from 'react-native';
import COLORS from 'styles/colors';

export default StyleSheet.create({
  text: {
    height: 50,
    alignItems: 'center',
    justifyContent: 'center',
  },
  button: {
    borderRadius: 15,
    borderWidth: 2,
    borderColor: COLORS.isabeline,
  },
});
